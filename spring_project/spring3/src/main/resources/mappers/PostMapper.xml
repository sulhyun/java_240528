<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.spring3.dao.PostDAO">
	<select id="selectCommunityList" resultType="kr.kh.spring3.model.vo.CommunityVO">
		SELECT * 
		FROM
			COMMUNITY 
		ORDER BY CO_NUM
	</select>
	<select id="selectPostList" resultType="kr.kh.spring3.model.vo.PostVO">
		SELECT *
		FROM 
			POST 
		WHERE 
			PO_CO_NUM = #{co_num}
		<choose>
			<when test="type == ''">
				AND (PO_TITLE LIKE CONCAT('%', #{search}, '%') OR 
				PO_ME_ID LIKE CONCAT('%', #{search}, '%'))
			</when>
			<otherwise>
				AND ${type} LIKE CONCAT('%', #{search}, '%') 
			</otherwise>
		</choose>
		ORDER BY PO_NUM DESC
		LIMIT #{pageStart}, #{perPageNum}
	</select>
	<select id="selectPostTotalCount" resultType="int">
		SELECT COUNT(*) 
		FROM 
			POST 
		WHERE 
			PO_CO_NUM = #{co_num}
		<choose>
			<when test="type == ''">
				AND (PO_TITLE LIKE CONCAT('%', #{search}, '%') OR 
				PO_ME_ID LIKE CONCAT('%', #{search}, '%'))
			</when>
			<otherwise>
				AND ${type} LIKE CONCAT('%', #{search}, '%') 
			</otherwise>
		</choose>
	</select>
</mapper>